
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./database.db"
}

model Queue {
  id    Int     @id @default(autoincrement())
  mail String
}

model Account {
  id Int @id @default(autoincrement())
  totalSent Int @default(0)
}